# Local Run Outputs

This directory is reserved for generated analysis runs. Full run artifacts are
not committed because they can be large and may contain machine-specific or
dataset-derived information.

Selected qualitative examples intended for public documentation are stored in
[`assets/visual-example`](../../assets/visual-example/).

For local experiments, prefer the ignored `.local_outputs/` directory or a
custom output path outside the repository.
