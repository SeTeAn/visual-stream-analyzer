# Optional ML dependencies for the DINOv2 representation path.
#
# Current local alpha environment: CUDA-enabled PyTorch for NVIDIA GPU.
# Install from the official PyTorch CUDA 12.8 wheel index:
#
#   .\.venv\Scripts\python.exe -m pip install -r requirements-ml.txt
#
# DINOv2 model code and weights are not included here and are not downloaded by
# the project automatically. Pass local asset paths through the CLI arguments
# --dinov2-source and --dinov2-checkpoint.
--index-url https://download.pytorch.org/whl/cu128
torch==2.11.0+cu128
torchvision==0.26.0+cu128
