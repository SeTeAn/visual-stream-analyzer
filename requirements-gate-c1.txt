# Additional local benchmark dependencies for OCID Gate C1.
# Install requirements-ml.txt first so PyTorch comes from the pinned CUDA index,
# then install this file from the normal Python package index:
#
#   .\.venv\Scripts\python.exe -m pip install -r requirements-ml.txt
#   .\.venv\Scripts\python.exe -m pip install -r requirements-gate-c1.txt
#
# Model weights are local assets and are never downloaded by the benchmark tools.
transformers==5.14.1
psutil==7.2.2
